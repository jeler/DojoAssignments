<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Dojo Message Board</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h2>The Dojo Message Board</h2>
        <hr>
            <form action="/post_message" method="post">
                <p>Name:</p>
                <input type="text" name="name">
                <p>Message:</p>
                <textarea rows="4" cols="100" name="text"></textarea>
                <br><br>
                <input type="submit" value="Post Message">
            </form>
            <% if(typeof(results) != "undefined") { %>     
                <% for(var i =0; i<results.length; i++) { %>
                    <h2 class="text-primary">Name: <%= results[i].name %></h2>
                    <h2 class="text-primary">Message: <%= results[i].text %></h2>
                    <% for(var j= 0; j<results[i].comments.length; j++) { %>
                    <h5>Name: <%= results[i].comments[j].name %></h5>    
                    <h5>Comment: <%= results[i].comments[j].text %></h5>
                    
                    <% } %>
                    <form action="/post_comment/<%=results[i].id %>" method="post">
                        <p>Name:</p>
                        <input type="text" name="comment_name">
                        <p>Comment:</p>
                        <textarea rows="4" cols="50" name="comment"></textarea>
                        <br><br>
                        <input type="submit" value="Post Comment"> 
                    </form>
                    <% } %>
                    <% } %>
            <% if(typeof(errors) != "undefined") { %> 
                <% for(var x =0; x<errors.length; x++) { %>
                    <p><%= errors[x]%></p>
            <% } %>
            <% } %>
        </div>
</body>
</html>
